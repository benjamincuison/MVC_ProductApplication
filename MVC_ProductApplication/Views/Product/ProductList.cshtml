@model List<MVC_ProductApplication.Models.CUST_PROD_DTO>
@{
    ViewBag.Title = "ProductList";
    Layout = "~/Views/Shared/_DefaultEmpty.cshtml";
}
<link href="~/Content/bootstrap.min.css" rel="stylesheet" />
<h1>DashBoard</h1>
<table class="table">
    <tr>
        <th>Customer ID</th>
        <th>Customer Name</th>
        <th>Customer Email</th>
        <th>Product ID</th>
        <th>Product Name</th>
        <th>Product Quantity</th>
    </tr>
    @foreach (var dataTransferObject in Model)
    {
        <tr>
            <td>@dataTransferObject.CustomerID</td>
            <td>@dataTransferObject.CustomerName</td>
            <td>@dataTransferObject.CustomerEmail</td>

            <td>@dataTransferObject.ProductID</td>
            <td>@dataTransferObject.ProductName</td>
            <td>@dataTransferObject.ProductQuantity</td>

        </tr>
    }
</table>
<div>
    <div >
        <div style="padding: 4px; float: left">
            <form action="@Url.Action("CreateProduct","Product")">
                <button type="submit"> Create Product </button>
            </form>
        </div>

        <div style="padding: 4px; float: left">
            <form action="@Url.Action("EditProduct","Product")">
                <button type="submit"> Edit Product </button>
            </form>
        </div>

        <div style="padding: 4px; float: left">
            <form action="@Url.Action("DeleteProduct", "Product")">
                <button type="submit"> Delete Product </button>
            </form>
        </div>

        <div style="padding: 4px; float:left">
            <form action="@Url.Action("CreateCustomer","Customer")">
                <button type="submit"> Create Customer </button>
            </form>
        </div>
        <div style="padding: 4px; float: left">
            <form action="@Url.Action("ProductList", "Product")">
                <button type="submit"> Product List </button>
            </form>
        </div>
    </div>
</div>
